import State from '../abstractions/State.js'

/**
 * @class Stopped
 * @classdesc The Stopped state is executed when the editor is stopped.
 * @extends State
 */
export default class Stopped extends State {
}
